## Packages
recharts | For visualizing bankroll history and game statistics
framer-motion | For smooth modal transitions and list animations
qrcode.react | To generate QR codes for session joining
lucide-react | Icon set (already in base, but ensuring it's noted)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
